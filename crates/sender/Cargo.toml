[package]
name = "transfer-sender"
version = "0.1.0"
edition = "2021"

[dependencies]
transfer-common = { path = "../common" }
qrcode = "0.14"
crossterm = "0.28"
clap = { version = "4", features = ["derive"] }
base64 = "0.22"
anyhow = "1"
